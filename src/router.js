import {Router}  from 'express';

const router = Router();

/**
 * Student
 */
router.post("/student", () => {})
router.get("/studentImage/:id", () => {})
router.put("/studentImage/:id", () => {})
router.delete("/studentImage/:id", () => {})

export default router;